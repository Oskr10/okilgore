# Resume

Drop the PDF here as:

```
oscar-kilgore-resume.pdf
```

That exact filename is what the nav link and the contact section point to.
Until the file exists, both are marked `TBA` in the interface.

Keep the filename stable across updates so shared links don't break.
